language: cpp

compiler: gcc

before_install:
  - sudo apt-get update
  - sudo apt-get install qt5-default qttools5-dev-tools

install:
  - qmake -project
  - qmake PingChat.pro
  - make
  
script:
# - ./Application
